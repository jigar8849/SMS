<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Login</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" />
    <style>
        .center-box {
            height: auto;
            width: auto;
        }

        .admin-login-container {
            height: 100vh;
            width: 100vw;
        }

        .login-page {
            height: 38rem;
            width: 33rem;
            border-radius: 0.5rem;
        }

        .input-box input {
            width: 30rem;
        }

        .submitButton button {
            width: 30rem;
        }
    </style>
</head>

<body>
    <div class="admin-login-container bg-primary d-flex justify-content-center align-items-center">

        <div class="center-box">

            <div>
                <a href="/" class="text-white text-decoration-none d-flex align-items-center">
                    <i class="fas fa-arrow-left me-2"></i> Back to Home
                </a>
            </div>

            <div class="login-page border bg-light">

                <div class="sild-icon d-flex justify-content-center mt-5">
                    <i class="fas fa-shield-alt text-primary fs-2 rounded-circle bg-primary p-3 bg-opacity-25"></i>
                </div>

                <h3 class="d-flex justify-content-center fw-bold">Admin Portal</h3>
                <p class="d-flex justify-content-center text-muted">Sign in to manage your society</p>

                <!-- ✅ Success / Error alert (optional) -->
                <div id="formSuccess" class="alert alert-success mx-4 d-none">Login successful!</div>
                <div id="formError" class="alert alert-danger mx-4 d-none">Please fill all fields correctly.</div>

                <% if (error && error.length> 0) { %>
                    <div class="alert alert-danger alert-dismissible fade show mx-4" role="alert">
                        <%= error[0] %>
                            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                    </div>
                    <% } %>


                        <% if(success && success.length){ %>
                            <div class="alert alert-success alert-dismissible fade show col-6 offset-3" role="alert">
                                <%= success %>
                                    <button type="button" class="btn-close" data-bs-dismiss="alert"
                                        aria-label="Close"></button>
                            </div>
                            <% } %>


                                <!-- ✅ Updated form with validation -->
                                <form id="adminLoginForm" action="/admin-login" method="post" class="needs-validation"
                                    novalidate>
                                    <div class="input-box ms-4 mt-5">
                                        <label for="email" class="fw-semibold mt-1">Email Address</label><br>
                                        <input type="email" name="email" id="email"
                                            class="form-control rounded p-2 mt-1" placeholder="admin@society.com"
                                            required>
                                        <div class="invalid-feedback">Please enter a valid email.</div>

                                        <label for="password" class="fw-semibold mt-3">Password</label><br>
                                        <input type="password" name="create_password" id="password"
                                            class="form-control rounded p-2 mt-1" placeholder="Enter your password"
                                            required>
                                        <div class="invalid-feedback">Password is required.</div>
                                    </div>

                                    <div class="action-box d-flex justify-content-between mt-4">
                                        <div class="left form-check ms-4">
                                            <input class="form-check-input" type="checkbox" value="" id="defaultCheck1">
                                            <label class="form-check-label" for="defaultCheck1">
                                                Remember me
                                            </label>
                                        </div>
                                        <div class="right me-4">
                                            <a href="#" class="text-decoration-none">Forgot password?</a>
                                        </div>
                                    </div>

                                    <div class="submitButton d-flex justify-content-center mt-3">
                                        <button class="btn p-2 rounded bg-primary text-white fw-bold fs-5"
                                            type="submit">Login
                                            as Admin</button>
                                    </div>
                                </form>

                                <div class="footer-text">
                                    <p class="d-flex justify-content-center mt-5 fs-5 mb-5 text-muted">Need to create a
                                        society
                                        account?<a href="/create-account" class="text-decoration-none ms-1">Register
                                            here</a>
                                    </p>
                                </div>

            </div>
        </div>
    </div>

    <script src="../js/validate.js"></script>

</body>

</html>