<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Create Society Account</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" />
  <style>
    body {
      background-color: #f8f9fa;
    }

    .card {
      border-radius: 1rem;
    }

    .form-label {
      font-weight: 500;
    }

    .btn-primary {
      border-radius: 0.5rem;
    }

    @media (max-width: 576px) {
      .card-body {
        padding: 2rem 1rem !important;
      }
    }
  </style>
</head>

<body>

  <div class="container py-5">
    <div class="mb-4">
      <a href="/" class="text-primary text-decoration-none d-flex align-items-center">
        <i class="fas fa-arrow-left me-2"></i> Back to Home
      </a>
    </div>

    <div class="card shadow">
      <div class="card-body p-5">
        <div class="text-center mb-5">
          <i class="fas fa-building fa-2x text-primary mb-3"></i>
          <h1 class="h3 fw-bold">Create Society Account</h1>
          <p class="text-muted">Set up your residential society management system</p>
        </div>
<% if (success && success.length > 0) { %>
  <div class="alert alert-success alert-dismissible fade show" role="alert">
    <%= success %>
    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
  </div>
<% } %>

<% if (error && error.length > 0) { %>
  <div class="alert alert-danger alert-dismissible fade show" role="alert">
    <%= error %>
    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
  </div>
<% } %>

        <form class="needs-validation" novalidate action="/create-account" method="POST" id="societyForm">
          <div class="bg-light p-4 rounded mb-4">
            <h4 class="mb-4">Society Information</h4>
            <div class="row g-3">
              <div class="col-12">
                <label class="form-label">Society Name</label>
                <input type="text" class="form-control" name="socity_name" required />
                <div class="invalid-feedback">Please enter society name.</div>
              </div>

              <div class="col-md-6">
                <label class="form-label">Total Flats</label>
                <input type="number" class="form-control" name="total_flat" required />
                <div class="invalid-feedback">Enter total flats.</div>
              </div>

              <div class="col-md-6">
                <label class="form-label">Total Blocks</label>
                <input type="number" class="form-control" name="total_block" required />
                <div class="invalid-feedback">Enter total blocks.</div>
              </div>

              <div class="col-md-6">
                <label class="form-label">Levels per Block</label>
                <input type="number" class="form-control" name="total_floor" required />
                <div class="invalid-feedback">Enter levels per block.</div>
              </div>

              <div class="col-md-6">
                <label class="form-label">Houses per Level</label>
                <input type="number" class="form-control" name="house_per_level" required />
                <div class="invalid-feedback">Enter houses per level.</div>
              </div>

              <div class="col-md-6">
                <label class="form-label">Four Wheeler Slots</label>
                <input type="number" class="form-control" name="total_four_wheeler_slot" required />
                <div class="invalid-feedback">Enter total 4-wheeler slots.</div>
              </div>

              <div class="col-md-6">
                <label class="form-label">Two Wheeler Slots</label>
                <input type="number" class="form-control" name="total_two_wheeler_slot" required />
                <div class="invalid-feedback">Enter total 2-wheeler slots.</div>
              </div>

              <div class="col-12">
                <label class="form-label">Society Address</label>
                <textarea class="form-control" name="socity_address" rows="3" required></textarea>
                <div class="invalid-feedback">Enter society address.</div>
              </div>

              <div class="col-md-4">
                <label class="form-label">City</label>
                <input type="text" class="form-control" name="city" required />
                <div class="invalid-feedback">Enter city name.</div>
              </div>

              <div class="col-md-4">
                <label class="form-label">State</label>
                <input type="text" class="form-control" name="state" required />
                <div class="invalid-feedback">Enter state name.</div>
              </div>

              <div class="col-md-4">
                <label class="form-label">Pincode</label>
                <input type="text" class="form-control" name="pincode" required />
                <div class="invalid-feedback">Enter pincode.</div>
              </div>
            </div>
          </div>

          <div class="bg-light p-4 rounded mb-4">
            <h4 class="mb-4">Admin Information</h4>
            <div class="row g-3">
              <div class="col-md-6">
                <label class="form-label">Admin Name</label>
                <input type="text" class="form-control" name="admin_name" required />
                <div class="invalid-feedback">Enter admin name.</div>
              </div>

              <div class="col-md-6">
                <label class="form-label">Admin Email</label>
                <input type="email" class="form-control" name="email" required />
                <div class="invalid-feedback">Enter a valid email.</div>
              </div>

              <div class="col-md-6">
                <label class="form-label">Admin Phone</label>
                <input type="tel" class="form-control" name="phone" required />
                <div class="invalid-feedback">Enter admin phone.</div>
              </div>

              <div class="col-md-6">
                <label class="form-label">Create Password</label>
                <input type="password" class="form-control" name="password" id="password" required minlength="6" />
                <div class="invalid-feedback">Password must be at least 6 characters.</div>
              </div>

              <div class="col-12">
                <label class="form-label">Confirm Password</label>
                <input type="password" class="form-control" name="confirm_password" id="confirm_password" required />
                <div class="invalid-feedback" id="confirmFeedback">Passwords must match.</div>
              </div>
            </div>
          </div>

          <div class="text-center">
            <button type="submit" class="btn btn-primary px-5 py-2 fs-5">Create Society Account</button>
          </div>
        </form>
      </div>
    </div>
  </div>

  <!-- Bootstrap + JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

<script src="../js/validate.js"></script>
</body>

</html>
